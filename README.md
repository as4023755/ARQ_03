# ARQ_03

A simple web-based bus reservation system built with Python (Streamlit) and MySQL.
Users can view bus schedules, book and cancel tickets, and check their bookings by email.
Admins can manage buses, routes, and schedules through an admin dashboard.
This project demonstrates frontend–backend integration and CRUD operations for a DBMS project.
